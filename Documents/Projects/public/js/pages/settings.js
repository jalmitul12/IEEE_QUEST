document.addEventListener('DOMContentLoaded',()=>{injectShell('Settings','Appearance, location privacy, accessibility and notification preferences.');document.getElementById('settings-form').addEventListener('submit',saveSettings);loadSettingsForm();});
async function loadSettingsForm(){try{const s=await loadSettings(),form=document.getElementById('settings-form');for(const name of ['theme','alertSeverity'])form.elements[name].value=s[name];for(const name of ['rememberLocation','notifications','reducedMotion','highContrast','compactDashboard','sound'])form.elements[name].checked=!!s[name];}catch(err){toast(err.message,'error');}}
async function saveSettings(e){e.preventDefault();const form=e.currentTarget,btn=form.querySelector('button[type=submit]');const body={theme:form.theme.value,alertSeverity:form.alertSeverity.value,rememberLocation:form.rememberLocation.checked,notifications:form.notifications.checked,reducedMotion:form.reducedMotion.checked,highContrast:form.highContrast.checked,compactDashboard:form.compactDashboard.checked,sound:form.sound.checked};setBusy(btn,true,'Saving…');try{const s=await api('/api/settings',{method:'PATCH',body:JSON.stringify(body)});AEGIS.settings=s;localStorage.setItem('aegisSettings',JSON.stringify(s));if(!s.rememberLocation)localStorage.removeItem('aegisLastLocation');applyPreferences(s);toast('Preferences saved.','success');}catch(err){toast(err.message,'error');}finally{setBusy(btn,false);}}
